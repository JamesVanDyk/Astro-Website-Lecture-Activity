// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	base: "/astro-website-lecture-activity", // replace 'repo-name' with your GitHub repository name
});
